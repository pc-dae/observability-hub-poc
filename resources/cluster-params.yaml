dnsSuffix: kubernetes.docker.internal
clusterIP: ${CLUSTER_IP}
storageClass: hostpath
namespaces:
 - vault
 - ingress-nginx
 - monitoring
 - kafka
 - argocd
 - grafana
 - otel-collector
 - cert-manager
caCert: |
  ${CA_CERT}