apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mimir
  namespace: argocd
spec:
  destination:
    namespace: monitoring
    server: https://kubernetes.default.svc
  project: default
  source:
    chart: mimir-distributed
    helm:
      values: |
        mimir:
          structuredConfig:
            memberlist:
              join_members:
                - '{{ printf "%s-gossip-ring.%s.svc.cluster.local" (include "mimir.fullname" .) .Release.Namespace }}'
            alertmanager:
              data_dir: /data/alertmanager
            ruler:
              alertmanager_url: http://localhost:8080/alertmanager
            query_scheduler:
              max_outstanding_per_tenant: 2048
            frontend_worker:
              scheduler_address: 'mimir-query-scheduler-headless.monitoring.svc.cluster.local:9095'
        querier:
          replicas: 2
        alertmanager:
          enabled: true
        rollout_operator:
          enabled: false
        nginx:
          enabled: true
        compactor:
          extraArgs:
            - "--config.file=/etc/mimir/mimir.yaml"
          command:
            - /bin/sh
            - -ec
            - |
              until nslookup $(MIMIR_MEMBERLIST_JOIN_MEMBERS); do
                echo "Waiting for memberlist DNS to be available..."
                sleep 3
              done
              /bin/mimir "$@"
    repoURL: https://grafana.github.io/helm-charts
    targetRevision: 5.1.0
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    - ServerSideApply=true
