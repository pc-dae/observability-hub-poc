apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: otel-host-metrics-dashboard
    files:
      - otel-host-metrics.json

commonLabels:
  grafana_dashboard: "1"

namespace: grafana
