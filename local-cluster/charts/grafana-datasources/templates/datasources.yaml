apiVersion: v1
kind: Secret
metadata:
  name: grafana-datasources
  namespace: grafana
  labels:
    grafana_datasource: "1"
stringData:
  datasources.yaml: |
    apiVersion: 1
    datasources:
    - name: Mimir
      type: prometheus
      uid: mimir
      url: https://mimir.{{ .Values.dnsSuffix }}/prometheus
      access: proxy
      isDefault: true
      jsonData:
        tlsSkipVerify: false
        tlsAuthWithCACert: true
        httpHeaderName1: "X-Scope-OrgID"
      secureJsonData:
        tlsCACert: |
{{ .Values.caCert | nindent 10 }}
        httpHeaderValue1: "fake"
    - name: Loki
      type: loki
      uid: loki
      url: https://loki.{{ .Values.dnsSuffix }}
      access: proxy
      jsonData:
        tlsSkipVerify: false
        tlsAuthWithCACert: true
        httpHeaderName1: "X-Scope-OrgID"
      secureJsonData:
        tlsCACert: |
{{ .Values.caCert | nindent 10 }}
        httpHeaderValue1: "fake"
    - name: Tempo
      type: tempo
      uid: tempo
      url: https://tempo.{{ .Values.dnsSuffix }}
      access: proxy
      jsonData:
        tlsSkipVerify: false
        tlsAuthWithCACert: true
        httpHeaderName1: "X-Scope-OrgID"
      secureJsonData:
        tlsCACert: |
{{ .Values.caCert | nindent 10 }}
        httpHeaderValue1: "fake"
    - name: Victoria Metrics
      type: prometheus
      uid: victoria-metrics
      url: http://victoria-metrics-victoria-metrics-cluster-vmselect.victoria-metrics:8481/select/0/prometheus/
      access: proxy
      jsonData:
        tlsSkipVerify: true
        httpHeaderName1: "X-Scope-OrgID"
      secureJsonData:
        httpHeaderValue1: "fake"
